# Fila de instruções — Claude Code

**Este arquivo é a fila viva.** Uma linha por instrução recebida do operador; a mais recente no
topo. Quando a fila passar de ~40 linhas, as concluídas migram verbatim para
`Docs/historico-fila-cc.md` (mesma regra do projeto irmão `ronatrip-website`: migrar não é
reescrever).

**Uma linha concluída nunca é reescrita** — o texto é permanente. A única célula que muda é o
**Estado** (e o **Commit**) da própria linha em andamento: `aguardando` → `concluido` com o hash
curto do commit de conteúdo, ou vazio se a instrução não gerou commit — nunca um hash chutado.
`cancelado` carrega o motivo em uma frase na descrição de uma linha nova.

**Instrução que emenda uma linha `aguardando` nasce linha nova**, nunca reescreve a emendada, e
declara qual das duas vence onde se contradizem.

**Nenhuma célula contém o caractere de barra vertical** — é o separador de coluna. Controle que
precisaria dele é expresso por coluna ("a coluna Commit da linha lê `bfbe6f3`").

**Backlog nunca entra aqui** — vai para `Docs/backlog-conhecido.md`. A fila é só instrução
recebida do operador.

**A frase de lançamento é constante:** *"leia `Docs/fila-cc.md` e execute a linha `aguardando`
cuja descrição começa com …"*. O agente lê a linha, abre a spec que ela aponta, mede o passo 0,
escreve o plano em `scratchpad/<leva>/plano.md` (não commitado) e **só começa a alterar arquivo
depois de o operador revisar o plano.**

| Data | Descrição | Estado | Commit |
|---|---|---|---|
| 2026-09-15 | LEVA 04C — ZONES AND PLACES: executar `Docs/spec-04c-zones-and-places.md` INTEIRA, escrita pelo Claude Web em 15/09 na conversa 7. **APONTE POR ESTA DESCRIÇÃO, nunca por “a linha aguardando”.** **Executor: Claude Code, modelo mais forte** — a leva escreve seis telas novas de administração, muda o único lugar que precifica uma entrega e reescreve a marcação de um formulário que o operador já usa. **O QUE A LEVA FECHA:** a lista de hotéis que o cliente escolhe mora no código — as quatro zonas e os dez locais existem só em `Infrastructure/Seeding/CatalogSeedData.cs` a partir da linha 204, nada em `src/OrlandoUp.Web/Pages/Admin` escreve zona ou local, e o seeder se recusa a rodar sobre banco já semeado; o hóspede de um hotel fora da lista não consegue ser cotado em tela nenhuma, porque o seletor de local é obrigatório e não há opção que queira dizer outro lugar (achados A1 e A4 de `Docs/conferencia-leva-03.md`). **RECORTE:** zonas e locais no `/admin`, a opção de hotel fora da lista nas duas pontas, a taxa de entrega editável por zona e sobrescrevível por reserva, e os ajustes A2, A3 e A5 do formulário de lançamento. Cupom, regra de distância, atribuição de unidade e formulário público de reserva NÃO entram — a §12 da spec diz o motivo de cada um. **NÃO DEPENDE DE PERGUNTA ABERTA:** as três decisões de negócio foram respondidas pelo Rod em 15/09 e estão em `Docs/decisions.md` como D43, D44 e D45; Q6 e Q15 travam só a 03b, e a Q4 (imposto) continua aberta com toda zona em zero. **SEM MIGRATION:** as três tabelas existem desde `20260904233355_InitialCreate` com todas as colunas que esta leva escreve; se você se pegar rodando `dotnet ef migrations add`, o desenho saiu errado e isso é parada com relato. **ESTADO DA ÁRVORE AO RECEBER:** HEAD é **descendente de `6be7a01`** (`docs: resumo da conversa 6`), que por sua vez descende de `698c035` e de `53676c2`, o commit de conteúdo da leva 03; os commits que trazem esta spec vêm depois de `6be7a01` e são o HEAD esperado. **O `git status`, o `git ls-files` e a paridade com o remoto NÃO foram medidos por quem escreveu esta linha** — a máquina virtual da ponte continua sem montar a pasta (atualização do Windows de 8/9) e o revisor ficou sem shell no repositório. Meça os três no passo 0 e relate. Qualquer arquivo modificado ou não rastreado fora de `scratchpad/` é parada com relato; aviso de CRLF e índice desatualizado são possibilidades a ignorar, não estado afirmado. Há commits por empurrar e **o push é do Rod** — o remoto e as credenciais são dele, e commit empurrado não se reescreve porque o hash já pode estar gravado em artefato. **ARQUIVOS JÁ ESCRITOS PELO CLAUDE WEB NESTA CONVERSA (não alterar):** `Docs/spec-04c-zones-and-places.md`, `Docs/conferencia-leva-04c.md` (só a coluna Resultado muda, e é o operador quem escreve), `Docs/decisions.md` (D43, D44, D45), `Docs/open-questions.md` (Q3), e esta fila fora das colunas Estado e Commit desta linha. **ARQUIVOS QUE A FRENTE PODE ALTERAR:** a lista fechada é a §11.1 da spec — novos: as seis páginas de `Pages/Admin/Zones/` e `Pages/Admin/Locations/` com seus modelos, `Docs/controles/zones-places.tsv` e `Docs/relatorio-leva-04c-etapa-N.md`; modificados só no que a §11.1 diz: `CatalogWriter.cs`, `QuoteBuilder.cs`, `BookingWriter.cs`, `Domain/Booking.cs`, `CatalogSeedData.cs`, `Pages/Book/Index` nos dois arquivos, `Pages/Admin/Bookings/Create` nos dois arquivos, `_AdminLayout.cshtml`, `site.css`, os dois `.resx`, `AdminCrudTests.cs`, `SiteBehaviourTests.cs`, `SeedingTests.cs`, `admin-catalog.tsv` e `public-site.tsv` nas linhas remedidas, `Docs/architecture.md` (nota datada) e as colunas Estado e Commit desta linha. Arquivo fora disso é parada, **sem cardinal**. **NEGATIVOS POR COLUNA DO DIFF:** `Infrastructure/Data/Migrations/` inteira intocada, inclusive o snapshot; `Domain/DeliveryZone.cs`, `Domain/DeliveryLocation.cs`, `Domain/DeliveryZoneTranslation.cs` e as três configurações delas intocadas; `Domain/Availability.cs`, `Domain/Quote.cs`, `Domain/BookingRules.cs`, `Domain/BookingStatusRules.cs` e `AvailabilityQueries.cs` intocados; `Pages/Admin/Products`, `Pages/Admin/Units`, `Pages/Admin/Batteries` e `Pages/Admin/Settings` intocadas; `CLAUDE.md` e `Program.cs` intocados — se registrar página virar necessário, é parada com relato, nunca linha silenciosa. **ONDE O PLANO NASCE:** `scratchpad/leva04c/plano.md`, sem commit; a execução só começa depois da revisão do operador, que chega como nota `EMENDA-04C-NN` datada no topo da spec. **ONDE OS RELATOS DE PARADA NASCEM:** `Docs/relatorio-leva-04c-etapa-N.md`, commitados antes de pedir aprovação; **três paradas** — P0 plano, P1 as quatro telas de zona e local com a navegação, os recursos e os testes 1 a 3 da §9, P2 o endereço público, a taxa sobrescrita, os ajustes do formulário, o seed e o resto dos testes. **PASSO 0 (spec §11.3):** grep dos radicais novos `Admin/Zones`, `Admin/Locations`, `DeliveryFeeOverride`, `line-cards`, `other-hotel`, `Book_FieldAddress`, `Admin_ZoneKind` e `Admin_Handover` sobre `src/` e `tests/`, **esperado zero em todos**; o valor no HEAD inicial de cada controle alheio que a leva move (C05 e C06 de `admin-catalog`, C16 e C17 de `public-site`, C17 de `foundation`, C10 de `public-site`, C01 de `booking-core`); a proposta de `Docs/controles/zones-places.tsv` com `medir` rodado no HEAD inicial; a forma dos testes por reflexão ancorada em NOME; a contagem medida da lista de chaves interpoladas de `AdminCrudTests.cs` antes de mudá-la; e toda contradição entre spec e árvore relatada no plano. **CONTROLES QUE MAIS MORDEM NESTA LEVA:** o C05 do `admin-catalog.tsv` é a relação handler de POST menos auditoria e tem de continuar em **2** — cada um dos quatro handlers novos grava uma linha de auditoria, e handler que não grava é o defeito, não o controle; o C16 do `public-site.tsv` proíbe identificador de catálogo digitado em `src` fora de `CatalogSeedData.cs` e **recebe `other-hotel` no mesmo commit do seed**, junto do C17; o C17 do `foundation.tsv` proíbe coalescer ausência em zero em `src` inteiro — a taxa sobrescrita resolve contra a taxa da zona, nunca contra zero (a forma proibida não está escrita aqui porque ela contém o separador de coluna). **BANCO:** só o LocalDB `OrlandoUpDb`, e nenhum comando de dado roda sem `SELECT DB_NAME()` colado antes; nenhuma migration a aplicar. **Quem cria a zona `other-hotel` no banco do Rod é o Rod**, pela tela nova, nos itens 6 a 9 do roteiro de conferência — o seed alcança instalação nova e o host de teste, nunca um banco já semeado. **SEGREDOS:** nenhum novo. **EFEITOS EXTERNOS:** nenhum — nem e-mail nem Stripe; o teste que afirma que nenhum dos dois está registrado continua verde. **ASSERÇÃO DE SOMA, POR DELTA:** esta escrita acrescenta **1** linha à tabela da fila, nenhuma migra para o histórico, e o total sobe exatamente 1; durante a execução o total não muda. **ASSERÇÃO EM SALDO:** ao receber, **1** linha `aguardando` — esta; ao fim, **0**, e esta linha lê `concluido` com o hash curto do commit de conteúdo. **FECHAMENTO EM DOIS COMMITS.** **FIM DE SESSÃO:** `git status --short` inteiro e `git diff --stat`, mais `bash Docs/medir-controles.sh verificar` nos **SEIS** `Docs/controles/*.tsv`, relatado, nunca silenciado. | aguardando |  |
| 2026-09-12 | LEVA 03 — BOOKING CORE: executar `Docs/spec-03-booking-core.md` INTEIRA, escrita pelo Claude Web em 12/09 na conversa 6. **APONTE POR ESTA DESCRIÇÃO, nunca por “a linha aguardando”.** **Executor: Claude Code, modelo mais forte** — a leva cria as quatro tabelas de reserva que toda frente seguinte lê, a regra de disponibilidade que decide o que o site pode prometer, e ganha uma migration (`AddBookings`). **O QUE A LEVA FECHA:** o sistema conhece a frota inteira — dez máquinas, doze baterias, catorze carregadores — e não consegue prometer nada dela a ninguém: a palavra `Booking` aparece em **1** arquivo de `src` e `tests` (o `.resx`, dentro do texto “Booking opens soon”), e `Pages/Rentals/Details.cshtml:180` renderiza um botão desabilitado com essa frase. **RECORTE (D39):** esta leva é o NÚCLEO — schema, disponibilidade contando unidades E baterias E carregadores por dia com o intervalo de um dia (D40), cotação congelada, página pública `/book` de disponibilidade e preço, e reserva lançada pela equipe em `/admin/bookings`; Stripe, formulário público de reserva, e-mail, hold e reembolso são a leva 03b e NÃO entram aqui. **NÃO DEPENDE DE PERGUNTA ABERTA:** as quatro perguntas que a spec precisava foram respondidas em 12/09 (D39 a D42); Q6 e Q15 travam só a 03b. **ESTADO DA ÁRVORE AO RECEBER, medido em 12/09:** HEAD é **descendente de `6da3ff6`** (o commit que traz esta spec, o roteiro `Docs/conferencia-leva-03.md`, D39–D42 e esta linha vem depois dele e é o HEAD esperado); `git status --porcelain` **vazio**; `git ls-files` conta **210**, **143** sob `src/` e **16** sob `tests/`; `origin/main...main` lê **0 e 13**, treze commits por empurrar, e o push é seu. Quatro arquivos de controle somam **54 controles**: `admin-catalog.tsv` 12 no alvo, `public-site.tsv` 17 no alvo, `fleet-batteries.tsv` 7 no alvo, `foundation.tsv` 18 com **2 fora** — o C14 e o C15, que chamam `dotnet` e respondem 127 ao shell da ponte, o que não é portão vermelho. O agente mede os dois no passo 0; controle de verdade vermelho ao receber é parada. Aviso de CRLF e índice desatualizado não são divergência; qualquer outro arquivo modificado ou não rastreado fora de `scratchpad/` é parada com relato. **ARQUIVOS JÁ ESCRITOS PELO CLAUDE WEB NESTA CONVERSA (não alterar):** `Docs/spec-03-booking-core.md`, `Docs/conferencia-leva-03.md` (só a coluna Resultado muda, e é o operador quem escreve), `Docs/decisions.md` (D39 a D42), `Docs/open-questions.md` (Q3), `Docs/roadmap.md`, e esta fila fora das colunas Estado e Commit desta linha. **ARQUIVOS QUE A FRENTE PODE ALTERAR:** a lista fechada é a §11.1 da spec — novos: nove arquivos em `Domain/` (`Booking`, `BookingLine`, `BookingAddOn`, `BookingEvent`, `BookingStatusRules`, `Availability`, `Quote`, `BookingRules`, `DeliveryWindows`), quatro configurações, a migration e o Designer, quatro serviços em `Infrastructure/Data/` (`AvailabilityQueries`, `QuoteBuilder`, `BookingWriter`, `BookingTimeline`), `Pages/Book/Index`, `Pages/Admin/Bookings/{Index,Create,Details}`, `tests/OrlandoUp.Tests/FakeClock.cs`, `Docs/controles/booking-core.tsv`, `Docs/relatorio-leva-03-etapa-N.md`; modificados só no que a §11.1 diz: `Enums.cs`, `AppDbContext.cs`, `OperationalSettings.cs` e a configuração dela, o snapshot, `CatalogSeedData.cs` e `CatalogSeeder.cs`, `IClock.cs` e `SystemClock.cs`, `Program.cs`, `Pages/Rentals/Details.cshtml` (linhas 177–183), `_AdminLayout.cshtml`, `Pages/Admin/Index`, `Pages/Admin/Settings/Index`, os dois `.resx`, `site.css` se precisar, `SiteFactory.cs` e os seis arquivos de teste nomeados, `admin-catalog.tsv` e `fleet-batteries.tsv` nas linhas remedidas, `Docs/architecture.md` (nota datada, D6/03 e D7/03), `README.md` se um passo mudar, e as colunas Estado e Commit desta linha. Arquivo fora disso é parada, **sem cardinal**. **ONDE O PLANO NASCE:** `scratchpad/leva03/plano.md`, sem commit; a execução só começa depois da revisão do operador, que chega como nota `EMENDA-03-NN` datada no topo da spec. **ONDE OS RELATOS DE PARADA NASCEM:** `Docs/relatorio-leva-03-etapa-N.md`, commitados antes de pedir aprovação; **quatro paradas** — P0 plano, P1 migration escrita e NÃO aplicada, P2 domínio e regras com testes verdes e nenhuma tela, P3 tudo escrito com os CINCO `.tsv` verificados. **PASSO 0 (spec §11.3):** grep das palavras `Booking`, `BookingLine`, `BookingAddOn`, `BookingEvent`, `Availability`, `Quote`, `DeliveryWindow`, `FakeClock` sobre `src/` e `tests/`, **esperado 1 linha** (o valor do `.resx`); valor no HEAD inicial de cada controle alheio que a leva remede (C05 e C06 de `admin-catalog`, C06 de `fleet-batteries` que vai de 5 a 6 pelo `IsOverbooked`, C09 e C10 de `admin-catalog`, C10 de `public-site`, C17 e C18 de `foundation`); proposta do `Docs/controles/booking-core.tsv` com `medir` no HEAD inicial, dizendo quais irmãos de alcance vão de `nao` a `sim`; leitura do `CatalogSeeder.cs` para dizer se o seed reescreve `TurnaroundDays` em linha existente; lista dos testes que leem `UtcNow` direto, para o relógio falso não os mover; e toda contradição entre spec e árvore relatada no plano. **BANCO:** só o LocalDB `OrlandoUpDb`. **Quem aplica a migration é o operador**, depois da P1 aprovada, com `dotnet ef database update --project src/OrlandoUp.Web`; a migration é aditiva (quatro tabelas e uma coluna em `OperationalSettings`, sem padrão de banco permanente — a P1 lê `sys.default_constraints` depois). **Quem põe “Dias de intervalo” = 1 nos três produtos reserváveis é o operador, pela tela de produto** — o seed muda para 1, mas o banco que roda é cópia que o seed não refresca. O `dotnet ef migrations add` grava BOM UTF-8 e o hook recusa — tirar antes do `git add`. **SEGREDOS:** nenhum novo. **EFEITOS EXTERNOS:** nenhum nesta leva — nem e-mail nem Stripe; o teste afirma que nenhum dos dois está registrado. **PUSH: do operador. Motivo:** o remoto e as credenciais são dele, e commit empurrado não se reescreve porque o hash já pode estar gravado em artefato. **CONTROLES NEGATIVOS QUE MAIS MORDEM NESTA LEVA:** o C16 do `public-site.tsv` proíbe os onze identificadores de catálogo em `src/` fora de `CatalogSeedData.cs` — as páginas novas resolvem produto e zona por consulta, nunca por literal; o C17 do `foundation.tsv` proíbe `?? 0` — a cotação falha fechada, nunca coalesce; o C05 do `admin-catalog.tsv` conta handler de POST menos `.Record(` e tem de continuar em **2** — cada handler novo de reserva grava evento por `BookingTimeline.Record`. **ASSERÇÃO DE SOMA, POR DELTA:** esta escrita acrescenta **1** linha à tabela da fila, nenhuma migra para o histórico, e o total sobe exatamente 1; durante a execução o total não muda. **ASSERÇÃO EM SALDO:** ao receber, **1** linha `aguardando`; ao fim, **0**, e esta linha lê `concluido` com o hash curto do commit de conteúdo. **FECHAMENTO EM DOIS COMMITS.** **FIM DE SESSÃO:** `git status --short` inteiro e `git diff --stat`, mais `bash Docs/medir-controles.sh verificar` nos **CINCO** `Docs/controles/*.tsv`, relatado, nunca silenciado. | concluido | 53676c2 |
| 2026-09-09 | LEVA 04B — FLEET BATTERIES: executar `Docs/spec-04b-fleet-batteries.md` INTEIRA, escrita pelo Claude Web em 09/09 na conversa 5. **APONTE POR ESTA DESCRIÇÃO, nunca por “a linha aguardando”.** **Executor: Claude Code, modelo mais forte** — a leva cria a entidade que o fluxo de reserva da leva 03 vai contar, e ganha uma migration. **O QUE A LEVA FECHA:** a frota que o sistema conhece são dez máquinas e mais nada; a operação tem **doze baterias e catorze carregadores**, e a D36 calcula que com quatro scooters e seis baterias por modelo **a bateria acaba antes da scooter**. Um fluxo de reserva escrito sobre o modelo de hoje venderia uma segunda bateria que não existe. **NÃO DEPENDE DE PERGUNTA ABERTA:** a D36 e a D37 fecharam a Q14, e o único fato que faltava — a contagem de carregadores — chegou em 09/09 e é **14**. **ESTADO DA ÁRVORE AO RECEBER, medido em 09/09:** HEAD é **descendente de `75ec0b0`** (o commit que traz esta spec, o roteiro de conferência e esta linha vem depois dele e é o HEAD esperado); `git status --porcelain` **vazio**; `git ls-files` conta **187**, **127** sob `src/` e **16** sob `tests/`; `origin/main...main` lê **0 e 2**, dois commits da conversa 5 por empurrar, e o push é seu. Três arquivos de controle somam **47 controles**: `admin-catalog.tsv` 12 no alvo, `public-site.tsv` 17 no alvo, `foundation.tsv` 18 com **2 fora** — o C14 e o C15, que chamam `dotnet` e respondem 127 ao shell da ponte, o que não é portão vermelho. O agente mede os dois no passo 0; controle de verdade vermelho ao receber é parada. Aviso de CRLF e índice desatualizado não são divergência; qualquer outro arquivo modificado ou não rastreado fora de `scratchpad/` é parada com relato. **ARQUIVOS JÁ ESCRITOS PELO CLAUDE WEB NESTA CONVERSA (não alterar):** `Docs/spec-04b-fleet-batteries.md`, `Docs/conferencia-leva-04b.md`, `Docs/decisions.md` (D36 e D37), `Docs/open-questions.md` (Q14 fechada, Q15 aberta) e esta fila. **O ROTEIRO DE CONFERÊNCIA ESTÁ NA LISTA NEGATIVA:** ele nasce vazio e quem preenche é o operador. Conferência que o agente escreve é conferência que ninguém fez. **ARQUIVOS QUE A FRENTE PODE ALTERAR:** a lista fechada é a §11.1 da spec. Arquivo fora disso é parada, **sem cardinal**. **Os três `.tsv` existentes estão na lista NEGATIVA quanto a edição** — mas o C05 e o C06 do `admin-catalog.tsv` são permanentes e **se movem** com esta leva: a frente os REMEDE na linha existente, no mesmo commit, nunca criando linha paralela. **ONDE O PLANO NASCE:** `scratchpad/leva04b/plano.md`, sem commit; a execução só começa depois da revisão do operador. **ONDE OS RELATOS NASCEM:** `Docs/relatorio-leva-04b-etapa-N.md`, commitados ANTES de pedir aprovação. **TRÊS PARADAS OBRIGATÓRIAS (§0 da spec):** P0 plano depois do passo 0, carregando a resposta ao ponto aberto K1 (as etiquetas das doze baterias); P1 migration escrita e **não aplicada**, classificada pela skill `revisao-migration-efcore`, com as contagens medidas antes e o `sys.default_constraints` lido depois de aplicada — a lição da D35; P2 tudo escrito e suíte verde, antes do commit de conteúdo. Não há parada de arreio de teste: ele existe desde a leva 04 e é medido pelos controles C11 e C12. **PASSO 0 (spec §11.3):** ferramentas e `SELECT DB_NAME()` lendo `OrlandoUpDb`; `verificar` nos TRÊS `.tsv` com os 47 no alvo antes de tocar em nada; `quem-ancora` e `proibidos`; grep de `Battery`, `BatteryKind`, `OperationalSettings` e dos nomes do seed e do comando sobre `src/` e `tests/`, **esperado zero**; contagens de `Products`, `Units` e `AuditEntries`; proposta do `Docs/controles/fleet-batteries.tsv` com `medir` no HEAD inicial; confirmação de que a fixture semeia o que os testes novos leem; e toda contradição entre spec e árvore relatada no plano. **BANCO:** só o LocalDB `OrlandoUpDb`. **Quem aplica a migration é o operador**, depois da P1 aprovada, com `dotnet ef database update --project src/OrlandoUp.Web`; **quem roda o seed das doze baterias também é o operador**, porque escreve dado real de frota. O `dotnet ef migrations add` grava BOM UTF-8 e o hook recusa — tirar antes do `git add`. **SEGREDOS:** nenhum novo. **PUSH: do operador. Motivo:** o remoto e as credenciais são dele, e commit empurrado não se reescreve porque o hash já pode estar gravado em artefato. **CONTROLE NEGATIVO QUE MAIS MORDE NESTA LEVA:** o C16 do `public-site.tsv` varre `src/` atrás dos onze identificadores de catálogo e exclui **apenas** o arquivo de nome `CatalogSeedData.cs` — o arquivo de seed de baterias **não** é excluído, então ele alcança o produto por consulta e nunca por literal. **ASSERÇÃO DE SOMA, POR DELTA:** esta escrita acrescenta **1** linha à tabela da fila, nenhuma migra para o histórico, e o total sobe exatamente 1; durante a execução o total não muda. **ASSERÇÃO EM SALDO:** ao receber, **1** linha `aguardando`; ao fim, **0**, e esta linha lê `concluido` com o hash curto do commit de conteúdo. **FECHAMENTO EM DOIS COMMITS.** **FIM DE SESSÃO:** `git status --short` inteiro e `git diff --stat`, mais `bash Docs/medir-controles.sh verificar` nos **QUATRO** `Docs/controles/*.tsv`, relatado, nunca silenciado. | concluido | afd9f43 |
| 2026-09-07 | LEVA 04 — CATALOG ADMINISTRATION: executar `Docs/spec-04-admin-catalog.md` INTEIRA, escrita pelo Claude Web em 07/09 na conversa 4. **APONTE POR ESTA DESCRIÇÃO, nunca por “a linha aguardando”.** **Executor: Claude Code, modelo mais forte** — a leva muda o schema uma vez, cria o primeiro caminho de ESCRITA autenticado da aplicação e constrói o arreio de teste que toda leva seguinte herda. **O QUE A LEVA FECHA:** `/admin` só lê. Medido em 07/09: nenhum page model sob `src/OrlandoUp.Web/Pages/Admin/` tem handler de POST que toque o catálogo, e `Pages/Admin/Products/Index.cshtml` linha 13 imprime na tela a chave `Admin_ProductsReadOnly` dizendo isso. Hoje só se corrige conteúdo apagando o catálogo e semeando de novo. **POR QUE ANTES DA LEVA 03:** `Docs/decisions.md` D33 — a metade da fase 4 que lê reserva fica para depois; a que não lê nada de novo roda agora. **ESTADO DA ÁRVORE AO RECEBER, medido em 07/09:** HEAD é **descendente de `cacd725`** (o commit que traz esta spec e esta linha vem depois dele e é o HEAD esperado); `git status --porcelain` **vazio**; `git rev-list --left-right --count origin/main...main` lê **0 e 0**, nada a empurrar e nada a puxar; `git ls-files` conta **157** arquivos, **109** sob `src/`. Os dois `.tsv` de controle somam 35 controles e todos estavam no alvo em 07/09, EXCETO que o C14 e o C15 do `foundation.tsv` **não foram medidos pelo revisor** — eles chamam `dotnet build` e `dotnet test`, e `dotnet` não existe no shell da ponte, que responde 127 (command not found), o que não é portão vermelho. O agente mede os dois no passo 0 e relata o valor real; controle já vermelho ao receber é parada com relato. Aviso de CRLF do Windows e índice desatualizado não são divergência; qualquer outro arquivo modificado ou não rastreado fora de `scratchpad/` é parada com relato. **ARQUIVOS JÁ ESCRITOS PELO CLAUDE WEB NESTA CONVERSA (não alterar):** `Docs/spec-04-admin-catalog.md`, `Docs/decisions.md` (D33 e D34), `Docs/roadmap.md` (a nota da divisão da fase 4) e esta fila. **ARQUIVOS QUE A FRENTE PODE ALTERAR:** a lista fechada é a §11.1 da spec — os arquivos novos e modificados que ela enumera sob `src/OrlandoUp.Web/` e `tests/OrlandoUp.Tests/`, mais `Docs/controles/admin-catalog.tsv`, `Docs/conferencia-leva-04.md`, `Docs/relatorio-leva-04-etapa-N.md` e as colunas Estado e Commit desta linha. Arquivo fora disso é parada, **sem cardinal**. **Os dois `.tsv` que já existem estão na lista NEGATIVA:** se a frente deslocar um controle deles, é parada com proposta de emenda, nunca edição silenciosa. **ONDE O PLANO NASCE:** `scratchpad/leva04/plano.md`, sem commit; a execução só começa depois da revisão do operador, e a mensagem ao humano é só o caminho do arquivo. **ONDE OS RELATOS DE PARADA NASCEM:** `Docs/relatorio-leva-04-etapa-N.md`, commitados ANTES de pedir aprovação. **QUATRO PARADAS OBRIGATÓRIAS (§0 da spec):** P0 plano depois do passo 0 e antes de alterar arquivo, carregando as respostas aos três pontos abertos da §10; P1 migration escrita e **não aplicada**, com o script classificado pela skill `revisao-migration-efcore` e as contagens de linhas das quatro tabelas medidas antes; P2 o arreio de teste da §8.1 verde ANTES da primeira tela de CRUD existir, com as três provas exigidas (GET autenticado 200, POST com token aceito, o mesmo POST sem token recusado); P3 tudo escrito e suíte verde, antes do commit de conteúdo. **POR QUE O P2 É PARADA E NÃO DETALHE:** medido em 07/09, a varredura de `tests/` por `AuthenticationHandler`, `ClaimsPrincipal`, `Antiforgery`, `RequestVerificationToken` e `CookieContainer` devolve **nada** — nenhum teste hoje alcança página de admin que não seja `/admin/login`, e nenhum faz POST de formulário. Sem o P2, todo comportamento novo desta leva nasce sem medição. **PASSO 0 (spec §11.3):** `dotnet --list-sdks`, `dotnet ef --version`, `sqllocaldb info`, `SELECT DB_NAME()` pela conexão configurada lendo `OrlandoUpDb`; `bash Docs/medir-controles.sh verificar` nos DOIS `.tsv` existentes com os 35 no alvo antes de tocar em qualquer coisa; `quem-ancora` sobre cada arquivo que a §11.1 declara novo ou modificado, e `proibidos` antes de escrever o primeiro comentário; grep dos identificadores que a frente vai criar (`AuditEntry`, `AuditAction`, e os nomes escolhidos para o serviço de escrita e para o handler de teste) sobre `src/` e `tests/`, **esperado zero** — qualquer retorno é parada e o nome muda antes de o tipo existir; contagens de linhas de `Products`, `AddOns`, `DeliveryZones` e `DeliveryLocations`; proposta de `Docs/controles/admin-catalog.tsv` com `medir` rodado no HEAD inicial; confirmação de que a fixture semeia tudo o que os testes novos leem; e toda contradição entre a spec e a árvore relatada no plano. **BANCO:** só o LocalDB `OrlandoUpDb`; não existe produção antes da fase 5 e nenhum comando de dado roda fora do local sem `SELECT DB_NAME()` colado antes. A migration desta leva tira o padrão de banco de quatro colunas booleanas e cria uma tabela de auditoria; **quem a aplica é o operador**, depois da P1 aprovada, com `dotnet ef database update --project src/OrlandoUp.Web`. O `dotnet ef migrations add` grava BOM UTF-8 e o hook de pre-commit recusa — tirar antes do `git add`, nos três arquivos que ele escreve. **SEGREDOS:** nenhum novo; os dois user-secrets da leva 01 já estão postos e são do operador. Se faltar algum, o agente imprime o comando só com o nome da chave e espera — nunca com valor inventado nem marcador de lugar. **PUSH: do operador, não do agente. Motivo:** o remoto e as credenciais são dele, e commit empurrado não pode mais ser reescrito porque o hash já pode estar gravado em artefato. **CONTROLES NEGATIVOS POR COLUNA DO DIFF:** `Program.cs`, `CatalogSeedData.cs`, `CatalogSeeder.cs`, `CatalogQueries.cs`, `Application/Catalog/CatalogViews.cs`, `TranslationPicker.cs`, `RichText.cs`, `PricingTierRules.cs`, `Api/SitemapEndpoints.cs`, os quatro arquivos de `Infrastructure/Localization/`, `appsettings.json`, `CLAUDE.md`, `Docs/decisions.md`, `Docs/architecture.md`, `Docs/roadmap.md`, `Docs/open-questions.md`, `Docs/market-notes.md`, `Docs/backlog-conhecido.md`, `Docs/protocolo-conversa.md`, `Docs/regras-de-controle.md`, `Docs/medir-controles.sh`, os resumos e atritos, `Docs/spec-01-foundation.md`, `Docs/spec-02-public-site.md`, as conferências e relatórios das levas 01 e 02, `Docs/controles/foundation.tsv`, `Docs/controles/public-site.tsv`, `.githooks/pre-commit`, `.gitattributes`, `.github/workflows/ci.yml`, e todo arquivo de teste exceto os dois que a §11.1 nomeia como modificados, aparecem em ZERO linhas do `git diff --stat` da faixa de commits da leva. Esta fila, só nas colunas Estado e Commit desta linha. **ASSERÇÃO DE SOMA, POR DELTA:** a escrita desta linha acrescenta **1** linha à tabela da fila, nenhuma migra para o histórico, e o total sobe exatamente 1. Durante a execução o total **não muda**. **ASSERÇÃO EM SALDO:** ao receber, esta fila tem **1** linha `aguardando`; ao fim, **0**, e esta linha lê `concluido` com o hash curto do commit de conteúdo. **FECHAMENTO EM DOIS COMMITS:** o de conteúdo primeiro (código, testes, migration, recursos, CSS, o `.tsv` novo, a conferência); depois o de fechamento, que grava o hash do primeiro na coluna Commit desta linha. **FIM DE SESSÃO:** `git status --short` inteiro e `git diff --stat`; `bash Docs/medir-controles.sh verificar` nos TRÊS `Docs/controles/*.tsv` relatado, nunca silenciado. | concluido | 594ed39 |
| 2026-09-05 | LEVA 02 — PUBLIC SITE: executar `Docs/spec-02-public-site.md` INTEIRA, escrita pelo Claude Web em 05/09 na conversa 3. **Executor: Claude Code, modelo mais forte** — a leva troca toda a superfície visível (paleta, tipografia, texto, catálogo) e mexe no schema uma vez; tudo o que um visitante vai ler nasce aqui. **ESTA É A ÚNICA LINHA `aguardando` DESTA FILA.** **O QUE A LEVA FECHA:** o site roda, mas todo número e toda frase nele são placeholder declarado (cabeçalho de `CatalogSeedData.cs`), a paleta é a direção A aposentada pela D29, e não existem áreas de entrega nem encanamento de máquina (sitemap, dados estruturados). **ESTADO DA ÁRVORE AO RECEBER, medido em 05/09:** HEAD é **descendente de `6ce1d10`** (o commit de fechamento da conversa 3 vem depois dele e é o HEAD esperado); `git status --porcelain` **vazio**; `git ls-files` conta **136** arquivos, 98 sob `src/`; `Docs/controles/foundation.tsv` tem 18 controles e todos verdes. Aviso de CRLF do Windows e índice desatualizado não são divergência; qualquer outro arquivo modificado ou não rastreado fora de `scratchpad/` é parada com relato. **ARQUIVOS JÁ ESCRITOS PELO CLAUDE WEB NESTA CONVERSA (não alterar):** `Docs/spec-02-public-site.md`, `Docs/decisions.md` (D32), `Docs/open-questions.md` (Q13), `Docs/resumo-conversa-3.md` e esta fila. **ARQUIVOS QUE A FRENTE PODE ALTERAR:** a lista fechada é a §12.1 da spec — tudo sob `src/OrlandoUp.Web/` e `tests/OrlandoUp.Tests/` que as §§4–9 nomeiam, `Docs/controles/public-site.tsv`, `Docs/conferencia-leva-02.md`, `Docs/relatorio-leva-02-etapa-N.md`; e, só no que a §12.1 diz, o **rótulo** do controle C16 de `Docs/controles/foundation.tsv` (troca a pergunta aberta Q9, já fechada, por Q12 — tipo, alvo, padrão e esperado não mudam) e as colunas Estado e Commit desta linha. Arquivo fora disso é parada, **sem cardinal**. **ONDE O PLANO NASCE:** `scratchpad/leva02/plano.md`, sem commit; a execução só começa depois da revisão do operador. **QUATRO PARADAS OBRIGATÓRIAS (§0 da spec):** P0 plano depois do passo 0 e antes de alterar arquivo, carregando as respostas aos três pontos abertos da §11; P1 migration escrita e **não aplicada**, com o script classificado pela skill `revisao-migration-efcore` e as contagens de linhas medidas antes; P2 fontes baixadas ou recusadas pela rede, com URL, licença e tamanho; P3 tudo escrito e testes verdes, antes do commit de conteúdo. Em cada uma o relato é commitado **antes** de pedir aprovação. **PASSO 0 (spec §12.3):** `dotnet --list-sdks`, `dotnet ef --version`, `sqllocaldb info`, `SELECT DB_NAME()` pela conexão configurada lendo `OrlandoUpDb`; `bash Docs/medir-controles.sh verificar Docs/controles/foundation.tsv` com os 18 verdes **antes** de tocar em qualquer coisa (controle já vermelho ao receber é parada); contagens atuais das tabelas do catálogo; um `curl -sI` por família de fonte; proposta de `Docs/controles/public-site.tsv` com `medir` rodado no HEAD inicial; a lista exata dos arquivos de `Pages/` que hoje leem a bandeira de cultura e as contagens de `asp-page` e `asp-route-culture`, para os controles C08 e C09 nascerem medidos e não de memória; contradição entre spec e árvore vai no plano. **BANCO:** só o LocalDB `OrlandoUpDb`; a migration `AddIsBookableAndOptionalDimensions` é aditiva (uma coluna `bit` com padrão e duas colunas que passam a aceitar nulo) e só é aplicada depois de P1 aprovada; o bloco destrutivo que apaga o catálogo placeholder é a §5.4 da spec, roda uma vez, imprime `SELECT DB_NAME()` antes, não toca em tabela do Identity e não existe como comando que fica no produto. **SEGREDOS:** nenhum novo — os dois user-secrets da leva 01 já estão postos; se faltar algum, o agente imprime o comando só com o nome da chave e espera. **PUSH: do operador, não do agente.** **CONTROLES NEGATIVOS POR COLUNA DO DIFF:** `CLAUDE.md`, `Docs/decisions.md`, `Docs/architecture.md`, `Docs/roadmap.md`, `Docs/open-questions.md`, `Docs/market-notes.md`, `Docs/backlog-conhecido.md`, `Docs/protocolo-conversa.md`, `Docs/regras-de-controle.md`, `Docs/medir-controles.sh`, os resumos e atritos, `Docs/spec-01-foundation.md`, `Docs/conferencia-leva-01.md`, os relatórios da leva 01, a spec desta leva, `.githooks/pre-commit`, `.gitattributes` e `.github/workflows/ci.yml` aparecem em zero linhas do `git diff --stat` da faixa de commits da leva; `Docs/controles/foundation.tsv` aparece com no máximo uma linha alterada, e é o rótulo do C16; esta fila, só nas colunas Estado e Commit desta linha. **ASSERÇÃO EM SALDO:** ao receber, esta fila tem **1** linha `aguardando`; ao fim, **0** `aguardando` e esta linha `concluido` com o hash do commit de conteúdo; o total de linhas da tabela **não muda**. **FECHAMENTO EM DOIS COMMITS:** o de conteúdo (código, testes, recursos, fontes, controles, conferência) primeiro; depois o de fechamento, que grava o hash do primeiro na coluna Commit desta linha. **FIM DE SESSÃO:** `git status --short` inteiro e `git diff --stat`; `bash Docs/medir-controles.sh verificar` nos dois `Docs/controles/*.tsv` relatado, nunca silenciado. | concluido | 383fe27 |
| 2026-09-04 | LEVA 01 — APPLICATION FOUNDATION: executar `Docs/spec-01-foundation.md` INTEIRA, escrita pelo Claude Web em 04/09 na conversa 1. **Executor: Claude Code, modelo mais forte** — a leva cria o schema, o Identity e a localização que toda leva seguinte herda. **ESTA É A ÚNICA LINHA `aguardando` DESTA FILA.** **O QUE A LEVA FECHA:** o repositório tem 13 arquivos rastreados e nenhum código (`git ls-files` em `bfbe6f3`). **ESTADO DA ÁRVORE AO RECEBER, medido em 04/09:** HEAD é **descendente de `bfbe6f3`** (o commit de fechamento da conversa 1 vem depois dele e é o HEAD esperado); `git status --porcelain` **vazio** — tudo o que o Claude Web escreveu está commitado; remoto pode não existir ainda. Aviso de CRLF do Windows e índice desatualizado não são divergência; qualquer outro arquivo modificado ou não rastreado é parada com relato. **ARQUIVOS JÁ ESCRITOS PELO CLAUDE WEB (não alterar):** `CLAUDE.md`, `README.md`, `.gitattributes`, `.gitignore`, `.githooks/pre-commit`, `Docs/decisions.md`, `Docs/architecture.md`, `Docs/roadmap.md`, `Docs/market-notes.md`, `Docs/open-questions.md`, `Docs/backlog-conhecido.md`, `Docs/protocolo-conversa.md`, `Docs/regras-de-controle.md`, `Docs/medir-controles.sh`, `Docs/spec-01-foundation.md`, `Docs/resumo-conversa-1.md`, `Docs/atrito-conversa-1.md`, e esta fila. **ARQUIVOS QUE A FRENTE PODE ALTERAR:** a lista fechada é a §9.1 da spec — `OrlandoUp.sln`, tudo sob `src/OrlandoUp.Web/` e `tests/OrlandoUp.Tests/`, `.github/workflows/ci.yml`, `Docs/controles/foundation.tsv`, `Docs/conferencia-leva-01.md`, `Docs/relatorio-leva-01-etapa-N.md`; e, só no que a §9.1 diz, `README.md`, `.gitignore` e as colunas Estado e Commit desta linha. Arquivo fora disso é parada, **sem cardinal**. **ONDE O PLANO NASCE:** `scratchpad/leva01/plano.md`, sem commit; a execução só começa depois da revisão do operador. **ONDE OS RELATOS DE PARADA NASCEM:** `Docs/relatorio-leva-01-etapa-N.md`, commitados antes de pedir aprovação. **PASSO 0 (spec §9.3):** `dotnet --list-sdks`, `dotnet ef --version`, `sqllocaldb info`; grep do radical `OrlandoUp` fora de `Docs/`, `README.md` e `CLAUDE.md` esperado 0; `ls Docs/controles` vazio (nenhum controle alheio a deslocar); proposta de `Docs/controles/foundation.tsv` com `medir` rodado no HEAD inicial; contradição entre spec e árvore vai no plano. **BANCO:** só o LocalDB `OrlandoUpDb` existe; a migration `InitialCreate` e os comandos `seed-catalog` e `seed-admin` rodam nele; não há outro banco e nenhuma connection string entra em arquivo commitado. **SEGREDOS:** os dois user-secrets (`ConnectionStrings:DefaultConnection`, `AdminSeed:Email` e `AdminSeed:Password`) são do operador — o agente imprime os comandos só com os nomes das chaves e espera. **PUSH: do operador, não do agente** — o remoto e as credenciais são dele e um commit empurrado não pode mais ser reescrito (o hash pode já estar gravado em artefato). **CONTROLES NEGATIVOS POR COLUNA DO DIFF:** os arquivos "já escritos pelo Claude Web" acima aparecem em zero linhas do `git diff --stat` da faixa de commits da leva, exceto `README.md` (seção "Running locally") e esta fila (colunas Estado e Commit desta linha). **ASSERÇÃO EM SALDO:** ao receber, esta fila tem **1** linha `aguardando`; ao fim, **0** `aguardando` e esta linha `concluido` com o hash do commit de conteúdo; o total de linhas da tabela **não muda** (nenhuma linha nova nasce nesta leva). **FECHAMENTO EM DOIS COMMITS:** o de conteúdo (código, testes, `foundation.tsv`, `conferencia-leva-01.md`, README) primeiro; depois o de fechamento, que grava o hash do primeiro na coluna Commit desta linha. **FIM DE SESSÃO:** `git status --short` inteiro e `git diff --stat`; `bash Docs/medir-controles.sh verificar Docs/controles/foundation.tsv` relatado, nunca silenciado. | concluido | 5d538ba |
